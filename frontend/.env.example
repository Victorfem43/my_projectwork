# For production (e.g. Vercel): set to your backend API URL (e.g. https://api.sieger.it.com/api)
# For local: leave unset if using server.js (port 3000); or set to http://localhost:5000/api if backend runs separately
NEXT_PUBLIC_API_URL=
